# Welcome to StrudleOS
## what is it?
StudleOS is a working operating sysem built on penguinmod windows has become bad and linux is more focused on development then MacOS is only for apple IMacs so there are not any good day-to-day OSes any more until now!

# how to install
To install download the version of StrudleOS beta test electron app the extract the contents of the .zip file then 
### for windows
open the StrudleOS.exe file
### for linux

# features
below is a list of important features

### apps
App in StrudleOS will have will hame two main types, currently the only supports one app type the HTML app type

### HTML app type
the HTML app type is the most basic and the only supported apps type currently. the HTML app type is a HTML file stored under you ~apps/ folder. they are able to run offline by being put in a iFrame using Blobs.
### FCC app type
the FCC app type is an app built from the native command system called Fidget Command Core it is not yet supported
